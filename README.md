# Node.js Application with Docker

This project demonstrates how to containerize a Node.js application using Docker. It includes steps for building, running, and pushing the Docker image to Docker Hub.
## Prerequisites

- Docker installed on your machine.
- Node.js and npm installed.

About
This project demonstrates how to containerize a Node.js application using Docker. It includes steps for building the Docker image, running the application in a container, and pushing the image to Docker Hub for distribution. The project is designed to help developers understand the basics of Docker and how it can be used to manage and deploy applications in isolated environments.

## Project Setup

1. Clone this repository:
2. ```bash
   **https://github.com/AditiPandey568/nodejs-application.git**
    
    ```
   ## Docker Setup

### 1. Build Docker Image

To build the Docker image for the Node.js app, run:

```bash
sudo docker build -t my-node-app .
